# 웹 타로 앱 프로젝트 기획 기록

- 문서 상태: 공개 저장소 등록 · Cloudflare 운영 배포 완료
- 최초 작성일: 2026-07-15
- 프로젝트 폴더명: `cloudflare-tarot`
- 앱 이름: `타로밀크티 웹`
- 이 문서의 목적: 대화에서 합의된 결정과 아직 결정하지 않은 항목을 구분해 기록한다.

## 1. 현재까지 합의된 프로젝트 방향

개인적으로 사용할 수 있는 웹 기반 타로 카드 앱을 만든다. 사용자가 질문하고 카드를 뽑으면 AI가 질문과 카드 조합을 바탕으로 한국어 해석을 생성한다.

사용자에게 표시할 앱 이름은 `타로밀크티 웹`으로 확정한다. Cloudflare 배포 이름은 `tarot-milktea`이며, 무료 운영 주소는 `https://tarot-milktea.cha-amu.workers.dev`이다.

초기 버전은 취미·개인 프로젝트로 운영하며 다음 원칙을 따른다.

- 사용자 화면은 정적 웹사이트로 제작한다.
- 유료 호스팅과 유료 AI API는 사용하지 않는다.
- 개인 도메인은 구매하지 않는다.
- 무료 제공 주소를 사용한다.
- 오픈소스 AI 모델을 직접 호스팅하지 않는다.
- 프로젝트가 실제로 인기를 얻거나 무료 한도가 부족해질 때만 유료 서비스 또는 개인 도메인을 다시 검토한다.

## 2. 확정된 기술 결정

### 2.1 호스팅

- 서비스: Cloudflare
- 프런트엔드: Cloudflare Worker의 정적 자산과 같은 배포 단위의 API Route
- 배포 프로젝트명: `tarot-milktea`
- 무료 주소: `https://tarot-milktea.cha-amu.workers.dev`
- 개인 도메인: 초기 범위에서 제외

### 2.2 AI 실행 방식

- 서비스: Cloudflare Workers AI
- 호출 구조: 정적으로 렌더링되는 프런트엔드에서 AI를 직접 호출하지 않고 같은 프로젝트의 서버 API Route를 거쳐 Workers AI 바인딩 호출
- 과금 정책: Workers AI 무료 할당량 안에서만 사용
- AI 제공자를 나중에 교체할 수 있도록 호출 코드는 UI와 분리

예상 요청 흐름:

```text
사용자 브라우저
  -> Cloudflare의 정적 React 화면
  -> 같은 출처의 /api Worker Route
  -> Cloudflare Workers AI
  -> 구조화된 JSON 응답
```

정적 사이트만으로 AI API를 직접 호출하면 보호 로직을 우회할 수 있다. 따라서 화면은 정적으로 렌더링하되 같은 출처의 Worker API Route를 입구로 사용한다. AI 바인딩, 세션 비밀값과 Turnstile 비밀값은 브라우저 번들에 포함하지 않는다. 현재 구현은 Vinext가 정적 화면과 API Route를 하나의 Cloudflare Worker 배포 단위로 묶는 구조이며, 별도 공개 AI Worker URL은 만들지 않는다.

해석 결과는 카드별 해석, 종합 해석과 그래프 데이터를 함께 검증해야 하므로 토큰 단위 스트리밍 대신 구조화된 JSON으로 받는다. 기다리는 동안 화면에는 `질문 분석`, `리딩 구성`, `카드 해석`, `결과 정리`처럼 현재 처리 단계를 표시한다.

### 2.3 해석 모델

Cloudflare Workers AI의 동일 입력 비교를 거쳐 기본 모델을 다음과 같이 확정한다.

- 모델: `@cf/google/gemma-4-26b-a4b-it`
- 호출 설정: 모델 내부 사고 과정은 출력하지 않도록 `enable_thinking: false` 적용
- 실패 처리: 형식·품질 검사를 통과하지 못하면 같은 Gemma 모델에 오류 사유를 전달해 최대 두 번 다시 요청한다. 정상 응답은 첫 호출에서 끝나며, 재시도는 근거 없는 인과관계나 구조 누락이 검출된 경우에만 사용한다.
- 선택 이유: 한국어 문장이 비교적 자연스럽고, 카드의 정·역방향 원뜻에서 질문과 자리 역할을 거쳐 판단 기준으로 이어지는 설명이 가장 명확했다.

동일한 한국어 질문, 카드와 구조화 출력 조건으로 실제 Workers AI 응답을 비교한 결과는 다음과 같다.

| 모델 | 관찰 결과 | 결정 |
|---|---|---|
| Gemma 4 26B-A4B | 약 4.3초, JSON 성공, 카드 원뜻과 질문의 인과 연결이 가장 분명함 | 기본 모델 |
| Qwen3 30B-A3B | 약 4.8초, JSON 성공, 결론이 추상적이고 원뜻과 질문 사이 설명이 약함 | 교체 |
| GLM 4.7 Flash | 약 10.2초, JSON 성공, 설명이 반복되는 경향 | 제외 |
| GPT-OSS 120B | 약 6.4초, 설명은 상세하지만 입력에 없는 메뉴를 만들어냄 | 기본 모델에서는 제외, 현재 품질 검사를 유지한 Groq 한도 대체 모델로만 사용 |
| Kimi K2.6 | 연결 문장은 자연스럽지만 입력에 없는 상황을 추가하고 무료 사용량 대비 비용과 지연이 큼 | 제외 |

모델 비교는 생성 문장만 보고 끝내지 않는다. 한국어 자연스러움, 카드 원뜻 보존, 질문·자리와의 연결 이유, 근거 없는 사실 추가 여부, 반복 표현, 응답 속도와 무료 사용량을 함께 평가한다. 모델을 바꾸더라도 카드 의미 데이터와 품질 검사는 같은 서버 계층을 사용한다.

### 2.4 타로 카드 이미지

- 이미지 출처: [채아무 자료 아카이브](https://cha-amu.github.io/archive/)
- 원본 manifest: [assets.json](https://cha-amu.github.io/storage/manifests/assets.json)
- 로컬 원본 보관 위치: `assets/cards/source/`
- 로컬 manifest: `assets/cards/manifest.json`
- 웹 공개용 경로: `public/cards/by-id/<card-id>.png`
- 무결성 확인 파일: `assets/cards/SHA256SUMS.txt`
- 프로젝트 포함일: 2026-07-16

아카이브에서 타로 태그가 붙은 PNG 이미지 79장을 프로젝트에 포함했다.

- 표준 타로 덱: 78장
  - 메이저 아르카나 22장
  - 마이너 아르카나 56장
- 추가 테마 카드: `밀크티` 1장
- 표준 카드 크기: `512×768`
- 추가 테마 카드 크기: `512×512`
- 전체 이미지 용량: 5,568,797 bytes

실제 앱의 기본 덱에는 메이저 아르카나 22장과 마이너 아르카나 56장을 합친 표준 78장 전체를 사용한다. 추가 테마 카드 `밀크티`는 기본 덱에 넣지 않고 앱 로고에 사용한다. 원본의 한글·기호 파일명은 운영체제마다 정규화 방식이 다르므로, 앱은 manifest로 원본을 검증한 뒤 카드 ID 기반 영문 공개 파일명으로 연결한다.

모든 카드 이미지는 사용자가 직접 제작한 작업물이며, 사용자가 이 프로젝트에서의 사용을 확정했다. 따라서 이 프로젝트에서 이미지를 사용하는 데 별도의 외부 라이선스 제약은 없다.

### 2.5 카드 의미 데이터

카드 의미를 모델의 기존 지식에만 맡기지 않고, 표준 78장의 해석 참고 데이터를 프로젝트 파일로 작성해 관리한다. 현대 타로 사이트의 문장을 복사하지 않으며, 공개 도메인에 해당하는 역사적 자료와 일반적인 카드 상징을 참고해 이 앱의 중립적 문체로 새로 작성한다.

- 구현 파일: `src/data/tarot-cards.ko.json`
- 데이터 수: 메이저 22장과 마이너 56장, 총 78개 항목
- 기본 필드: 카드 ID, 한국어·영문 이름, 아르카나, 슈트와 숫자·계급
- 해석 필드: 정방향·역방향 핵심어, 기본 의미, 주의점, 질문 분야별 관점
- 질문 분야: 관계, 진로·학업, 선택·행동, 개인 상태
- 별도 검증 스크립트로 78장 누락, 중복 ID와 필수 필드를 검사

AI에는 전체 78장 데이터를 매번 보내지 않고 실제로 선택된 카드의 데이터만 전달한다. AI는 이 데이터를 기준으로 질문, 자리 역할과 카드 조합을 연결하며, 데이터에 없는 내용을 확정적인 카드 의미처럼 추가하지 않는다.

### 2.6 구현 프레임워크

프런트엔드는 `React + TypeScript + Vite` 기반 Vinext로 구현했다. 카드 선택, 순차 공개, 후속 질문, 그래프 갱신과 로컬 기록처럼 상태 변화가 많은 단일 페이지 앱을 정적으로 렌더링하면서 같은 프로젝트에 Cloudflare Worker API Route를 둘 수 있는 구성이다.

- UI 프레임워크: React
- 언어: TypeScript strict mode
- 빌드 도구: Vite + Vinext
- 스타일: 전역 디자인 토큰과 반응형 CSS
- 화면 상태: React 상태 훅과 명시적인 TypeScript 상태 타입
- 요청·응답 검증: Zod 스키마를 프런트엔드와 Worker에서 공유
- 그래프: 외부 차트 테마에 의존하지 않고 반응형 SVG로 직접 구현
- 단위·컴포넌트 테스트: Vitest와 React Testing Library
- 주요 사용자 흐름 테스트: 서버 렌더링 테스트와 배포 전 실제 브라우저 확인
- Cloudflare 설정 및 로컬 실행: Wrangler

서버 코드는 TypeScript 기반 Worker API Route로 작성했다. 프런트엔드 컴포넌트, AI 제공자 연결, 카드 데이터와 API 스키마를 분리해 모델이나 호스팅 구성이 바뀌어도 UI 전체를 다시 작성하지 않도록 했다.

### 2.7 봇 감지와 과다 요청 방지

로그인이 없으므로 리딩을 시작할 때 Cloudflare Turnstile의 `Managed` 방식을 통과하게 한다. 검증에 성공하면 유효 시간 2시간의 서명된 익명 세션을 발급하고, 정상적인 후속 질문에는 Turnstile을 반복해서 요구하지 않는다. 세션이 만료되거나 비정상 요청이 감지되면 다시 검증한다.

보호 계층은 다음과 같이 구성한다.

- 브라우저의 `Origin` 헤더가 있는 `POST /api/*` 요청은 같은 출처인지 검사하며, `Origin`이 없는 요청도 서명 세션·JSON 형식·요청 제한을 모두 통과해야 함
- JSON Content-Type, 요청 본문 크기와 Zod 스키마 검사
- 리딩 설계 질문 최대 500자, 카드 해석에 전달되는 질문 문맥 최대 1,000자
- 카드 ID, 카드 수, 정역방향과 이미 선택된 카드 중복 여부를 서버에서도 검사
- 한 리딩 세션에서 최대 6회의 AI API 요청만 허용
  - 최초 리딩 설계 1회와 최초 해석 1회
  - 후속 질문 2회에 대해 설계와 해석을 각각 최대 1회
- 형식·품질 오류가 있으면 한 API 요청 안에서 설계는 최대 2회, 해석은 최대 3회 모델 추론을 시도하므로 전체 사용자 흐름의 실제 모델 추론은 최악의 경우 최대 15회가 될 수 있음
- 세션 기준 분당 10회, 익명화한 네트워크 기준 분당 30회의 Worker Rate Limiting 적용
- 공유 네트워크 사용자를 과도하게 차단하지 않도록 네트워크 기준 제한은 세션 기준보다 느슨하게 적용
- D1에는 세션 ID의 해시, 만료 시각, AI 호출 수와 후속 질문 횟수만 저장
- 사용자 이름, 질문, 선택 카드, AI 응답과 원본 IP 주소는 D1 및 운영 로그에 저장하지 않으며, AI 검증 실패 로그에는 시도 횟수와 고정된 오류 분류만 기록
- 제한 초과 시 HTTP 429와 재시도 가능 시각을 반환

Turnstile만으로 모든 자동 요청을 막을 수 있다고 가정하지 않는다. Turnstile, 서명된 세션, 기능상 호출 상한, Worker Rate Limiting과 입력 검증을 함께 사용한다.

## 3. 질문 입력과 카드 선택 경험

### 3.1 전체 리딩 흐름

1. 사용자가 자연어로 질문을 입력한다.
2. AI가 질문을 분석해 이번 리딩에 필요한 카드 수와 각 카드 자리의 역할을 정한다.
3. 화면에 카드가 뒷면으로 늘어뜨려진 덱을 보여준다.
4. 사용자가 덱에서 직접 카드를 선택한다.
5. 선택한 카드를 공개한다.
6. AI가 원래 질문, 리딩 구조, 선택된 카드를 함께 해석한다.

카드는 AI가 자동으로 뽑지 않는다. 사용자가 펼쳐진 덱에서 직접 선택하는 경험을 핵심으로 한다. 카드 수와 과거·현재·조언 등의 자리 역할은 고정하지 않고 질문에 맞춰 AI가 구성한다.

정방향과 역방향 카드를 모두 사용한다. 리딩 세션의 덱을 만들 때 각 카드에 정방향 또는 역방향을 각각 50% 확률로 독립적으로 배정한다. 배정된 방향은 같은 세션 동안 바뀌지 않으며 카드가 공개되기 전까지 사용자에게 보이지 않는다.

### 3.2 AI 호출 단계

AI 처리는 두 단계로 분리한다.

#### 1단계: 리딩 설계

사용자 질문을 전달하고 구조화된 형식으로 다음 정보를 받는다.

- 이번 리딩에 필요한 카드 수
- 각 카드 자리의 이름과 살펴볼 관점
- 사용자에게 보여줄 짧은 카드 선택 안내

최초 리딩에서 AI는 질문의 범위와 복잡도에 따라 1~5장 사이에서 필요한 카드 수를 정한다. 각 카드에는 질문에 맞는 자리 이름과 살펴볼 관점을 부여한다.

#### 2단계: 종합 해석

다음 정보를 함께 전달한다.

- 사용자의 원래 질문
- 1단계에서 만든 리딩 구조
- 사용자가 선택한 카드와 각 카드의 방향
- 카드별 기준 의미 데이터

AI는 카드마다 분리된 설명만 나열하지 않고, 질문과 카드 사이의 흐름을 종합해 해석한다.

#### 질문별 답변 계약

특정 메뉴나 질문 사례를 코드에 하나씩 추가하지 않는다. 설계 AI가 질문 전체의 의미와 사용자가 요구한 결과 형태를 읽고 다음 공통 계약 중 하나를 만든다.

| 계약 | 결과 |
|---|---|
| `choose_one` | 사용자가 직접 제시한 후보 중 하나를 선택 |
| `recommend_one` | 구성 단계에서는 후보를 만들지 않고 질문별 역할 카드를 배정한 뒤, 공개된 카드 근거로 구체적인 답 하나를 생성 |
| `yes_no` | 예·아니오 중 하나로 먼저 답변 |
| `outcome` | 성공·실패, 합격·불합격, 성사 여부처럼 결과 한쪽을 직접 제시 |
| `compare` | 후보를 억지로 하나 고르지 않고 핵심 차이를 비교 |
| `forecast` | 가장 가능성이 큰 흐름이나 시기를 직접 제시 |
| `advice` | 먼저 실행할 행동을 제시 |
| `explain` | 중심 원인을 제시 |
| `analysis` | 가장 중요한 상태나 관계를 제시 |

계약에는 `kind`, `subject`, `candidates`, `decisive`를 저장한다. 사용자가 직접 적은 2~5개 선택지는 서버가 추출해 보존하고 선택·예/아니오·비교 질문에서만 후보별 카드를 배정한다. 열린 추천의 `candidates`는 항상 빈 배열이며, 구성 단계에는 판단 역할만 정하고 구체적인 추천은 카드 공개 뒤 해석 AI가 생성한다. 로컬 분류는 AI를 사용할 수 없을 때의 보조 수단이며, AI 응답에서는 “추천”, “왜”, “비교”처럼 사용자가 답의 형태를 명시한 경우와 계약의 공통 불변 조건만 검증한다. 카드 해석 본문이나 실제 추천 답을 로컬 문구로 덮어쓰지 않는다.

해석 결과에는 `verdict.kind`, `verdict.value`, `verdict.statement`를 둔다. 명시적 선택·예/아니오 질문은 보존된 후보 하나를 `value`로 반환한다. 열린 추천은 미리 만든 후보를 고르는 대신 카드 근거를 해석한 뒤 질문에 맞는 구체적인 답 하나를 `value`로 새로 생성한다. 결과 질문은 `성공`, `실패`, `합격`, `불합격`처럼 카드가 가리키는 한쪽을 `value`로 반환한다. 모든 직접 답은 `summary`의 첫 문장을 같은 `statement`로 시작하며, 비교·전망·조언·설명·분석도 각 계약이 요구하는 핵심 차이, 흐름, 행동, 원인 또는 발견을 첫 문장에서 말한다.

AI가 작성한 카드별 설명과 종합문을 고정 템플릿으로 덮어쓰지 않는다. 서버는 카드 ID, 정·역방향, 자리, 원뜻과 원자료 표시만 확정하고, 직접 답 누락·후보 이탈·카드 근거 누락·추상문·내부 필드 노출을 품질 검사로 거부해 AI에 수정 이유를 전달한다. 카드 상징에 근거한 현실 추정은 허용하므로 음식, 옷, 관계, 진로, 일정 등 질문 분야가 달라져도 같은 검증 절차를 사용한다.

후속 질문 문맥은 원 질문, 이전 후속 질문, 이전 직접 결론과 이전 답변 계약을 구조화해 전달한다. 명시적 후보 선택의 앞선 결론을 가리키는 후속 질문만 기존 후보를 이어받는다. 열린 추천은 후보가 아니라 이전에 생성한 답과 카드 문맥을 사용하며, 새 주제를 묻는 후속 질문은 이전 후보와 그래프 축을 사용하지 않는다.

### 3.3 질문 입력 화면 문구 초안

#### 제목

> 무엇에 대해 타로를 보시겠습니까?

#### 안내 문구

> 질문을 자연어로 입력하세요. 현재 상황과 확인하고 싶은 내용을 구체적으로 작성하면 질문에 맞는 카드 수와 해석 구조를 결정합니다.

#### 입력창 예시 문구

> 예: 이직을 결정하기 전에 고려해야 할 점은 무엇인가?

#### 입력 도움말

> 관계, 진로, 선택, 현재 상황 등 원하는 주제를 입력할 수 있습니다. 상황과 알고 싶은 내용을 함께 작성하면 더 구체적으로 해석할 수 있습니다.

#### 질문 예시

- 현재 관계에서 반복되는 문제는 무엇인가?
- 새로운 일을 시작하기 전에 고려해야 할 점은 무엇인가?
- 두 선택지 중 하나를 결정할 때 어떤 기준을 우선해야 하는가?
- 최근 피로가 반복되는 상황에서 점검해야 할 요소는 무엇인가?
- 현재 상황에서 놓치고 있는 가능성은 무엇인가?

예시 질문은 입력창을 대신하지 않는다. 사용자가 예시에 없는 어떤 질문도 자신의 말로 자유롭게 입력할 수 있다는 점이 분명히 보이도록 구성한다.

질문 입력 전에는 사용자의 상황 정보가 없으므로 예시 문구를 AI로 매번 생성하지 않는다. 무료 AI 호출량을 예시에 소비하지 않도록 여러 주제를 포함한 검수된 고정 예시를 사용한다. AI는 사용자가 실제 질문을 제출한 뒤에 질문의 의도, 카드 수, 자리 역할과 해석 기준을 결정하는 데 사용한다.

질문 입력 화면에서는 감성적인 표현, 위로하는 표현, 신비주의적인 표현을 사용하지 않는다. 사용자가 무엇을 입력해야 하는지 직접 설명하는 기능 중심의 문장을 사용한다.

### 3.4 카드 방향 및 공개 방식

카드를 모두 선택한 후 자리 순서대로 한 장씩 공개한다.

1. AI가 정한 수만큼 사용자가 뒷면 카드를 선택한다.
2. 선택된 카드를 AI가 정한 자리 순서대로 뒷면 상태로 배치한다.
3. 사용자가 빛나는 첫 번째 카드 자체를 누르면 카드를 공개한다.
4. 공개된 카드의 자리 역할, 카드명, 정방향·역방향 여부와 개별 해석을 표시한다.
5. 사용자가 다음 카드 자체를 눌러 직접 공개한다.
6. 마지막 카드까지 공개한 후 전체 카드의 종합 해석을 표시한다.

자동 시간 간격으로 카드를 넘기지 않는다. 사용자가 직접 다음 카드 공개를 진행한다. 연출을 건너뛰고 싶은 사용자를 위해 `전체 공개` 기능을 함께 제공한다. 후속 질문에서 추가 카드를 뽑을 때도 같은 공개 방식을 사용한다.

카드의 방향은 공개 시점에 새로 추첨하지 않는다. 세션 시작 시 이미 배정된 방향을 사용한다. 카드 뒷면 UI는 실제 방향을 드러내지 않고, 공개할 때 역방향 카드의 앞면만 180도 회전해 표시한다.

### 3.5 후속 질문과 추가 카드

최초 카드 해석이 끝난 뒤 사용자는 원하는 경우 자연어로 후속 질문을 입력할 수 있다. 후속 질문은 필수가 아니며, 한 리딩 세션당 최대 2회까지 허용한다. 후속 질문은 새로운 리딩으로 초기화하지 않고 기존 리딩의 연장으로 처리한다.

후속 질문 흐름은 다음과 같다.

1. 사용자가 최초 해석을 확인한다.
2. 방금 해석에서 더 살펴보고 싶은 내용을 자연어로 질문한다.
3. AI가 기존 질문, 리딩 구조, 선택된 카드와 해석을 참고해 추가 카드가 필요한지 판단한다.
4. 추가 카드가 필요하면 AI가 후속 질문의 범위와 복잡도에 따라 카드 수와 각 카드의 역할을 정한다.
5. 사용자가 이미 뽑은 카드를 제외한 남은 덱에서 직접 추가 카드를 선택한다.
6. AI가 기존 리딩과 추가 카드를 연결해 후속 해석을 생성한다.

같은 리딩 세션에서는 이미 선택한 카드가 다시 나오지 않는다. 후속 해석은 추가 카드만 따로 설명하지 않고, 이전 카드와 해석에서 이어지는 맥락을 함께 다룬다.

#### 후속 질문 화면 문구 초안

> 추가로 확인할 내용이 있습니까?

> 후속 질문을 입력하세요. 필요한 경우 추가 카드를 선택하고 이전 해석과 함께 분석합니다.

입력창 예시:

> 예: 이 상황에서 우선할 행동은 무엇인가?

후속 질문은 한 리딩 세션당 최대 2회로 제한한다. 추가 카드 수는 고정하지 않으며, 각 후속 질문의 내용에 따라 AI가 다르게 결정한다. 한 번의 후속 질문에서 추가할 수 있는 카드는 최대 5장으로 제한한다.

### 3.6 AI 해석 문체

AI 해석은 사람처럼 대화하거나 감정을 표현하는 문체를 사용하지 않는다. 카드와 질문 사이의 관계를 설명하는 중립적이고 분석적인 문체를 사용한다.

문체 원칙은 다음과 같다.

- 해석의 근거가 되는 카드, 방향, 자리 역할을 명시한다.
- 카드별 의미와 카드 사이의 관계를 구분해 설명한다.
- 결과를 묻는 질문은 성공·실패, 합격·불합격, 일어남·일어나지 않음 중 카드가 더 강하게 가리키는 쪽을 첫 문장에서 직접 말한다.
- 불필요한 인사, 공감 표현, 위로, 감탄문과 대화형 추임새를 사용하지 않는다.
- AI가 감정이나 직관을 가진 것처럼 표현하지 않는다.
- 신비주의적 수사와 과장된 표현을 사용하지 않는다.
- 동일한 의미를 여러 표현으로 반복해 분량을 늘리지 않는다.
- 질문의 크기를 넘겨 해석하지 않는다. 식사·옷차림·오늘 일정 같은 질문을 재정, 조직, 인간관계나 장기 계획으로 확대하지 않는다.
- `sourceMeaning`은 모델이 새로 만들지 않고 서버가 카드 이름·방향·핵심어·기본 의미 데이터로 고정한다. 질문에 적용하는 문장에서는 일반 주의문과 기본 의미를 그대로 복사하지 않고 판단 기준과 행동으로 연결한다.
- `방향성`, `외부 조건`, `실행 가능성`, `요소가 상호작용한다`, `균형 있게 고려한다`처럼 질문 없이도 성립하는 추상문을 사용하지 않는다.
- 요약은 직접적인 결론, 카드별 해석은 각 카드의 근거, 종합 해석은 카드 관계, 확인할 점은 실제 행동으로 역할을 분리한다.

선택된 카드의 정·역방향 핵심어, 기본 의미와 주의점은 질문 종류와 관계없이 모델에 그대로 전달한다. 일상 질문이라는 이유로 카드 원뜻을 정규식 단서나 식사·옷차림 용어로 먼저 치환하지 않는다. 원뜻을 먼저 보존해야 모델이 실제 카드 근거에서 질문으로 연결할 수 있고, `기분 변화`의 `변화` 같은 일부 단어만 보고 전혀 다른 의미로 바꾸는 오류를 막을 수 있다.

각 카드의 구조화된 해석은 다음 순서를 반드시 가진다.

1. `text`: 지금 질문에 대한 짧은 결론
2. `reasoning.sourceMeaning`: 서버가 카드 이름, 방향, 핵심어와 기본 의미로 확정한 원뜻
3. `reasoning.questionConnection`: 그 원뜻이 정확한 자리 이름과 질문에 왜 연결되는지 설명
4. `reasoning.decisionImpact`: 이 카드가 전체 판단을 긍정·부정 중 어느 쪽으로 얼마나 움직이는지
5. `evidence`: 서버가 원자료에서 만든 `역방향 · 환상 · 기분 변화`, `자리 · 포만감` 같은 추적용 표시

`sourceMeaning`에서는 카드 원자료의 추상어를 설명할 수 있다. 대신 `questionConnection`과 `decisionImpact`에서는 그 의미를 질문의 실제 대상과 행동에 연결한다. 예를 들어 컵 기사 역방향의 `환상·기분 변화`를 식사 질문에 적용할 때는 “새 메뉴가 나쁘다”로 치환하지 않고, 순간적으로 당기는 메뉴와 먹은 뒤 필요한 식사량을 구분해야 하는 이유로 설명한다.

카드 상징에서 질문에 필요한 현실 내용을 적극적으로 추론한다. 맛·취향·포만감·영양·몸 상태·날씨·상대의 감정·합격과 성공 여부를 질문에 직접 쓰지 않았더라도 카드 원뜻과 자리 역할에서 자연스럽게 이어지면 구체적으로 말할 수 있다. 단, 실제 검사 결과나 공식 통계, 정확한 측정값, 의학적 진단을 받은 것처럼 가짜 출처를 만들지는 않는다.

Workers AI 응답에는 다음 품질 검사를 적용한다.

- 요청한 카드 수, 카드 ID, 자리 이름과 정역방향이 정확한지 검사
- 모델이 같은 카드 항목을 중복한 경우 정상 항목을 요청 순서대로 한 번만 사용
- 모든 카드에 원뜻·질문 연결·판단 영향의 세 설명이 있고, 원뜻이 서버가 확정한 카드 문장과 일치하는지 검사
- 질문 연결에 정확한 자리 이름과 충분한 설명이 있는지 검사
- 질문 범위를 벗어난 표현, 추상문, 적용 문장에서 카드 데이터 원문을 복사한 문장과 요약 반복을 거부
- 일상 질문의 요약, 질문 연결, 판단 영향, 행동 항목과 그래프 축이 질문 대상을 직접 언급하는지 검사
- 카드 상징에서 추론한 취향·감정·신체감각·미래 결과가 카드 원뜻과 자리 역할에 연결되는지 검사
- `positionFocus`, `sourceMeaning` 같은 내부 JSON 키가 사용자 문장에 노출된 출력을 거부
- 한국어 해석문을 자연스러운 해요체로 통일
- 사용자가 후보를 명시한 선택·예/아니오 질문은 후보마다 한 장을 배정하고, 요약 첫 문장에서 반드시 하나를 직접 선택하며 확인 항목에서 결론을 다시 조건부로 돌리지 않는지 검사
- 열린 추천은 구성 단계의 `candidates`가 비어 있고 자리가 질문별 판단 역할로 구성되는지, 카드 공개 뒤 해석에서 질문에 맞는 구체적인 답 하나를 새로 생성하는지 검사
- 비교 질문은 모든 후보를 보존하고 후보별 카드 자리를 배정하되, 사용자가 선택을 요구하지 않았다면 억지로 하나를 고르지 않는지 검사
- 결과·설명·전망·조언·분석은 각각 성공 또는 실패·실제 원인·시기나 방향·먼저 할 행동·핵심 발견을 첫 문장에 제시하는지 검사하고, 질문을 다시 말하거나 "확인한다"로 끝낸 답을 거부
- 형식이나 품질 검사를 통과하지 못한 경우 오류 이유를 포함해 보정 요청을 시도한다. 계약 후보가 이미 확정된 선택·예/아니오·비교 질문은 다시 실패해도 같은 계약의 로컬 해석으로 공개 흐름을 계속한다. 열린 추천·결과·전망·조언·설명·분석은 구조적으로 유효한 AI 응답을 한 번이라도 확보했다면 문체나 축 같은 부가 품질 검사 때문에 502로 폐기하지 않고 마지막 구조화 응답을 사용한다.
- 사용자 질문과 AI 원문은 운영 로그와 D1에 저장하지 않으며, 검증 실패는 상세 문장 없이 오류 분류만 기록

사용하지 않을 표현의 예:

- `카드가 당신에게 속삭이고 있습니다.`
- `당신의 깊은 마음이 느껴집니다.`
- `우주가 보내는 강력한 신호입니다.`
- `제가 곁에서 함께 살펴보겠습니다.`
- 카드나 자리 근거 없이 임의로 결과만 단정하는 표현

권장 표현의 예:

> 이번 카드 배열에서는 김치찌개 쪽을 골라요. 전차는 김치찌개 선택을 빠르게 확정하는 근거가 되고, 펜타클 왕은 반대 선택지를 검토할 때 확인할 내용을 보여 줘요.

해석의 최종 분량과 화면에 표시할 세부 항목은 3.11의 기준을 따른다.

### 3.7 AI 해석 시각화

AI 타로라는 성격을 명확하게 보여주기 위해 텍스트 해석과 함께 그래프 및 수치 지표를 제공한다.

수치는 AI가 질문·자리 역할·카드·정역방향의 관계를 분석한 결과를 정규화한 `AI 해석 지표`로 취급한다. 결과 질문에서는 성공·실패 중 어느 쪽이 강한지, 관계 질문에서는 감정의 강도가 어느 정도인지처럼 텍스트 결론을 보조한다.

초기 버전에서는 `동적 레이더 차트`와 `해석 신호 분포 막대`를 함께 사용한다.

#### 동적 레이더 차트

AI가 질문과 리딩 내용을 바탕으로 핵심 해석 항목을 3~5개 생성하고, 각 항목의 신호 강도를 0~100으로 표시한다. 모든 질문에 같은 항목을 강제로 사용하지 않고 질문에 관련된 항목으로 축을 구성한다.

예시:

- 변화 가능성 78
- 안정성 42
- 관계의 영향 65
- 정보의 불확실성 54

축이 6개 이상이면 작은 화면에서 구분하기 어렵고 항목의 의미도 중복될 수 있으므로 최대 5개로 제한한다. 각 항목에는 해당 수치의 근거가 된 카드 또는 카드 조합을 함께 표시한다.

#### 해석 신호 분포

전체 리딩에서 확인되는 신호를 합계 100인 가로 누적 막대로 표시한다.

- 진행을 지지하는 신호
- 주의가 필요한 신호
- 불확실하거나 추가 확인이 필요한 신호

`진행 45 · 주의 35 · 불확실성 20`과 같이 항목명과 수치를 그래프에 함께 표시한다. 결과형 질문도 같은 분포를 사용하되 텍스트에서는 카드가 가리키는 성공·실패 한쪽을 직접 말한다.

#### 결과 화면 배치

데스크톱에서는 카드 영역 아래에 텍스트 해석과 동적 레이더 차트를 나란히 배치한다. 해석 신호 분포 막대는 두 영역 아래에 표시한다.

```text
선택한 카드와 공개 영역

┌────────────────────┬──────────────────┐
│ 종합 해석           │ 동적 레이더 차트  │
│ 카드별 근거          │ 3~5개 해석 항목   │
└────────────────────┴──────────────────┘

[진행 신호] [주의 신호] [불확실성]
```

모바일에서는 카드, 텍스트 해석, 레이더 차트, 신호 분포 순서로 세로 배치한다. 그래프는 결과가 처음 나타날 때 한 번만 애니메이션으로 표시하며, 애니메이션이 없어도 모든 정보를 확인할 수 있어야 한다.

#### 후속 질문 전후 비교

추가 카드를 뽑은 경우 기존 레이더 차트를 흐린 선으로 남기고, 추가 카드를 반영한 차트를 진한 선으로 겹쳐 표시한다. 변경된 항목은 `안정성 42 → 61`과 같이 이전 값과 현재 값을 텍스트로도 제공한다.

후속 질문이 새로운 관점을 요구해 기존 항목과 직접 비교하기 어려운 경우에는 억지로 같은 축을 유지하지 않는다. 비교 가능한 항목만 변화량을 표시하고 새 항목은 별도로 구분한다. 신호 분포 막대도 이전 값과 현재 값을 함께 확인할 수 있게 한다.

#### 초기 버전에서 제외하는 시각화

질문, 카드, 자리 역할과 핵심 주제를 노드와 선으로 연결하는 카드 관계 그래프는 초기 버전에서 제외한다. 레이더 차트와 신호 분포만으로 근거 관계를 충분히 전달하기 어려울 때 후속 기능으로 다시 검토한다.

시각화 원칙:

- 모든 수치에는 근거가 된 카드 또는 카드 조합을 연결한다.
- `AI 해석 지표`, `신호 강도`, `판단 기울기` 등 질문에 맞는 명칭을 사용한다.
- 실제 조사나 검사에서 얻은 공식 통계처럼 가짜 출처를 붙이지 않는다.
- 텍스트 해석 없이 수치만 단독으로 제시하지 않는다.
- 그래프의 색상만으로 항목을 구분하지 않고 항목명과 수치를 함께 표시한다.
- 같은 입력에 대한 변동을 줄이도록 일관된 평가 기준과 낮은 생성 변동성을 사용한다.
- AI 응답은 항목명, 점수, 근거 카드, 신호 분포를 구조화된 데이터로 반환하고 프런트엔드가 그래프를 그린다.

### 3.8 리딩 구조 확인 방식

AI가 만든 카드 수와 각 자리 역할은 카드 선택 전에 사용자에게 표시한다. 사용자는 구조를 확인한 후 카드 선택으로 진행할 수 있지만 자리 수나 역할을 직접 수정하지는 않는다. 구조가 질문 의도와 다르면 질문 입력 화면으로 돌아가 질문을 수정해 다시 분석한다.

모호하거나 예·아니오 형식인 질문은 AI가 분석 가능한 관점으로 재구성하고, 구조 확인 화면에 `이번 리딩의 해석 기준`으로 표시한다. 별도의 확인 대화상자를 추가하지 않으며, 사용자는 그대로 진행하거나 질문을 수정할 수 있다.

### 3.9 UI 분위기와 글꼴

UI는 일반적인 웹 문서나 블로그가 아니라 한 화면씩 진행하는 타로 플래시 게임의 경험으로 구성한다. [타로시크릿 오늘의 운세](https://www.tarotcafe.net/tarot/today)의 단계 전환, 중앙 카드 무대, 하단 안내창과 직접 카드 선택 방식을 참고하되 화면과 문구는 그대로 복제하지 않는다.

- 전체 화면을 하나의 타로 무대로 사용하고 긴 세로 문서나 섹션 목차를 만들지 않는다.
- 앱 진입 시에는 두들 선화 중심의 타이틀 화면을 먼저 표시한다. 사용자는 여기에서 이름을 정하고 `타로 시작`을 눌러 질문 장면으로 들어간다.
- 기본 이름은 `ㅇㅁ`이며 사용자가 바꾼 이름은 해당 브라우저에만 저장한다.
- 질문 입력, 구조 확인, 셔플, 카드 선택, 순차 공개, 결과를 각각 독립된 장면으로 전환한다.
- 중앙에는 현재 행동 대상인 덱이나 카드만 크게 표시한다.
- 안내와 주요 버튼은 화면 아래의 게임 대화창 형태로 고정한다.
- 셔플 중에는 두 카드 더미가 교차해 하나로 합쳐지는 동작을 보여준다.
- 선택 단계에서는 78장 카드 뒷면을 스크롤 없는 얕은 부채꼴로 촘촘하게 펼친다. 가리키거나 선택한 카드는 위로 올라와 식별할 수 있어야 한다.
- 공개 단계에서는 별도 다음 버튼보다 카드 자체를 눌러 한 장씩 뒤집고 해당 카드 해석을 같은 화면에서 확인한다. 사용자는 전체 공개도 선택할 수 있다.
- 최종 결과는 긴 블로그 글 대신 요약, 카드별 해석, AI 지표를 탭으로 전환하는 콘솔에 표시한다.
- 이름, 질문 예시, 기록, 저장·공유와 후속 질문은 본 흐름을 방해하지 않도록 보조 버튼이나 오버레이에 둔다.
- 장식적인 점술가 연출이나 사람처럼 꾸민 말투 대신 `TAROT MILKTEA / SYSTEM`의 직접적이고 분석적인 안내를 사용한다.
- 카드 뒷면은 사용자 제작 `밀크티_타로_카드.png`를 흰색 카드 안에 맞춰 사용한다.
- 두들 분위기는 손그림 카드와 어울리는 비정형 이중 외곽선, 살짝 어긋난 단색 그림자와 손으로 그린 듯한 기능선으로 표현한다. 의미 없는 곡선이나 낙서 장식은 추가하지 않는다.

색상은 흰색, 검은색과 회색을 중심으로 하는 무채색 계열을 사용한다.

- 홈부터 질문, 선택, 공개와 결과까지 전체 무대에 동일한 미색 종이 배경을 사용한다.
- 본문, 제목, 주요 외곽선과 선택 표시에는 검은색 계열을 사용한다.
- 보조 문구, 비활성 상태와 영역 구분에는 단계가 다른 회색을 사용한다.
- 타로 카드 원본 이미지의 색상은 그대로 유지해 무채색 UI 안에서 카드가 중심으로 보이게 한다.
- 그래프는 명도, 선 종류, 채움 패턴, 항목명과 수치를 함께 사용해 색상에만 의존하지 않고 구분한다.
- 유채색 강조색은 기본 팔레트에 포함하지 않으며, 필요성이 생기면 별도로 결정한다.

기본 글꼴은 [오뮤 다예쁨체](https://noonnu.cc/font_page/1136)를 사용한다.

- CSS 글꼴 이름: `OmuDaye`
- 적용 범위: 앱 이름, 안내 문구, 입력창, 버튼, 카드명, 해석문과 그래프 라벨을 포함한 전체 UI
- 대체 글꼴: 글꼴을 불러오지 못했을 때 사용할 일반 한글 `sans-serif` 계열을 함께 지정
- 웹폰트 형식: WOFF2와 `font-display: swap` 사용
- 라이선스: 개인·기업 사용자에게 무료이며 웹사이트 사용과 서버 내 임베딩이 허용됨
- 제한 사항: 폰트 파일을 수정하거나 별도로 재판매·재배포하지 않음

손글씨 계열 글꼴을 사용하더라도 긴 AI 해석문의 크기와 줄 간격은 읽기 쉽게 설정한다. 그래프의 작은 수치와 라벨도 축소했을 때 식별 가능한지 구현 단계에서 확인한다.

### 3.10 선택형 익명 이름

로그인이나 계정을 만들지 않고, 질문 입력 화면에서 사용할 이름 또는 별칭을 선택적으로 입력할 수 있게 한다.

- 입력 항목: `이름 (선택)`
- 최대 길이: 앞뒤 공백을 제외한 20자
- 입력한 경우: `OO의 타로`, `OO의 리딩 결과`처럼 화면 제목에 사용
- 입력하지 않은 경우: 임의의 기본 이름을 붙이지 않고 `리딩 결과`로만 표시
- 저장 위치: 사용자의 브라우저 `localStorage`
- 사용자가 언제든 이름을 변경하거나 삭제할 수 있는 기능 제공
- HTML로 해석하지 않고 일반 텍스트로만 렌더링

이름은 화면을 구분하기 위한 로컬 표시 정보이며 AI 해석에 필요하지 않으므로 서버나 Workers AI에 전송하지 않는다. AI 해석문도 사용자의 이름을 반복해 부르지 않고 기존의 중립적인 문체를 유지한다.

### 3.11 AI 해석 결과 구성과 분량

최초 리딩 결과에는 다음 항목을 모두 포함한다.

1. 질문과 리딩 구조
2. 핵심 결론
3. 카드별 해석
4. 카드 조합의 종합 해석
5. 판단 기준과 주의할 점
6. 동적 레이더 차트와 해석 신호 분포
7. 각 수치의 근거 카드

분량 기준은 다음과 같다.

| 항목 | 기준 분량 |
|---|---|
| 핵심 결론 | 2~3문장, 약 120~220자 |
| 카드별 짧은 결론 | 카드 한 장당 한 문장, 약 25~80자 |
| 카드 원뜻 | 카드 한 장당 약 50~130자 |
| 질문·자리와의 연결 이유 | 카드 한 장당 약 80~180자 |
| 판단에 미치는 영향 | 카드 한 장당 약 50~130자 |
| 카드 조합의 종합 해석 | 3~5문장, 약 250~450자 |
| 판단 기준과 주의할 점 | 2~4개 항목, 항목당 한 문장 |
| 그래프 근거 | 레이더 축마다 한 문장, 약 40~90자 |

전체 분량은 카드 수에 따라 조절한다.

- 1~2장 리딩: 약 700~1,200자
- 3장 리딩: 약 900~1,500자
- 4~5장 리딩: 약 1,200~2,000자
- 후속 질문 해석: 약 600~1,200자

카드별 결과 화면은 `결론 → 카드의 원뜻 → 이 질문에서 → 판단에 미치는 영향 → 원자료 단서` 순서로 표시한다. 원뜻과 적용 이유를 한 문장으로 압축하지 않으며, 태그 두 개만으로 설명을 대신하지 않는다. 원자료 단서는 AI가 만든 주장형 해시태그가 아니라 서버가 선택 카드 데이터에서 확정한 방향·핵심어·자리 표시만 사용한다.

후속 해석에서는 최초 해석을 반복하지 않고, 추가 카드의 의미, 기존 해석에서 달라진 점, 수정된 결론과 그래프 변화에 집중한다. 결과 화면의 핵심 결론과 그래프는 바로 표시하고, 세부 그래프 근거는 `근거 보기`를 통해 펼칠 수 있게 해 화면이 지나치게 길어지지 않도록 한다.

### 3.12 진행 상태 복구, 저장과 공유

서버에는 질문과 결과를 저장하지 않으면서 다음 기능을 모두 제공한다.

- 진행 중인 질문, 리딩 구조, 선택 카드와 공개 단계는 `sessionStorage`에 자동 저장
- 같은 탭을 새로고침하면 진행 중인 리딩을 복구
- 완성된 결과는 사용자가 `이 기기에 저장`을 선택한 경우에만 IndexedDB에 저장
- 로컬 기록 목록, 개별 삭제와 전체 삭제 기능 제공
- 결과를 일반 텍스트로 복사
- 공유 버튼은 질문이나 해석 본문을 포함하지 않고 현재 사이트 주소만 복사
- 질문, 종합 해석, 전체 카드 해석과 AI 지표를 하나의 이미지 파일로 저장

주소 복사는 결과 내용을 외부로 내보내지 않는다. 이미지 저장 파일에는 질문, 별칭과 해석 내용이 포함될 수 있지만 서버에 업로드하지 않고 브라우저에서 직접 생성한다. 공개 공유 URL은 만들지 않으며, 브라우저의 로컬 기록은 로그인 없이 해당 기기와 브라우저에서만 볼 수 있다.

## 4. 무료 사용량 기준

2026-07-16에 확인한 Cloudflare 공식 문서를 기준으로 한다. 정책은 바뀔 수 있으므로 실제 구현 및 배포 시 다시 확인한다.

### 4.1 Workers AI

- 무료 할당량: 매일 10,000 Neurons
- 초기화 시각: 매일 00:00 UTC, 한국 시간 오전 9시
- 무료 한도 소진 시: 추가 과금 없이 이후 AI 요청 실패
- 텍스트 생성 기본 호출 한도: 분당 300회

Gemma 4 26B-A4B의 현재 Workers AI 모델 목록 표시 단가는 다음과 같다.

- 입력 100만 토큰당 USD 0.10
- 출력 100만 토큰당 USD 0.30

카드 의미와 설명 구조를 모두 포함한 최초 리딩 1회가 입력 4,000토큰과 출력 1,000토큰을 사용하고 자동 재요청이 없다고 가정하면, 무료 10,000 Neurons 안에서 하루 약 157회의 해석을 보수적으로 예상한다. 실제 카드 수, 후속 질문과 품질 재요청에 따라 횟수는 줄어든다.

실제 사용량은 시스템 프롬프트, 카드 설명, 사용자 질문, 생성된 해석 길이에 따라 달라진다. 구현 시 응답 토큰 상한을 설정하고 대시보드에서 실제 사용량을 확인한다.

### 4.2 Worker

- Workers Free 요청 한도: 하루 100,000회
- 이 프로젝트에서는 Worker 자체 요청 한도보다 Workers AI의 Neuron 한도가 먼저 소진될 가능성이 높다.

## 5. Cloudflare 우선·Groq 한도 대체 구조

동일한 `GPT-OSS 120B`를 사용하는 경우 Groq와 Cloudflare의 생성 능력은 같은 기반 모델에서 출발한다. 제공자의 추론 방식, 정밀도, 기본 파라미터 등에 따라 실제 출력은 조금 달라질 수 있다.

비교 결과는 다음과 같다.

| 항목 | Groq | Cloudflare Workers AI |
|---|---|---|
| GPT-OSS 120B 기준 무료 타로 해석량 추정 | 약 111회/일 | 약 115회/일 |
| 공개 생성 속도 | 약 500 tokens/sec | 모델별 TPS 미공개 |
| 정적 사이트에서 사용 | 별도 API 키와 중계 Worker 필요 | Worker AI 바인딩으로 연결 가능 |
| 서비스 구성 | Cloudflare + Groq 두 서비스 | Cloudflare 한 서비스 |
| 개인정보 처리 | 기본 추론 요청은 보관하지 않지만 장애·악용 조사 시 미국 GCP에 최대 30일 임시 기록 가능. 계정에서 ZDR 설정 가능 | 고객 콘텐츠를 학습·서비스 개선에 사용하지 않으며 별도 저장 서비스를 연결하지 않으면 저장하지 않음 |

Groq는 공개된 생성 속도가 빠르다는 장점이 있지만, 이 프로젝트에서는 다음 이유로 Cloudflare를 기본 공급자로 유지한다.

- Pages, Worker, AI를 한 플랫폼에서 관리할 수 있다.
- 별도의 외부 AI API 키가 필요하지 않다.
- 무료 사용량이 개인용 앱에 충분할 가능성이 높다.
- 개인정보 처리 조건이 개인적인 고민을 입력하는 타로 앱에 적합하다.
- 개인정보 처리와 운영 구성이 단순하다.

실제 개발·품질 테스트 중 하루 10,000 Neurons가 소진되어 테스트와 열린 질문 해석이 함께 중단되는 상황을 확인했다. 운영 중에는 정확한 일일 무료 할당량 초과뿐 아니라 Workers AI의 일시 오류와 의미 품질 보정에도 Groq를 사용하되, 정상 응답에서는 Workers AI를 계속 우선한다.

- 카드 구성: Groq `qwen/qwen3.6-27b`, 비추론 JSON Object Mode, 최대 900 출력 토큰. 2026년 7월 17일 종료되는 Llama 4 Scout와 Qwen3 32B는 사용하지 않는다.
- 카드 해석: Groq `openai/gpt-oss-120b`, 첫 호출 Strict JSON Schema, 최대 3,600 출력 토큰
- 해석 보정: 첫 해석이 스키마 또는 품질 검사를 통과하지 못하면 더 작은 모델로 낮추지 않고 같은 `openai/gpt-oss-120b`를 사용한다. 첫 Groq 호출은 Strict JSON Schema, 이후 보정은 JSON Object Mode로 생성하고 서버 Zod 검증을 적용한다.
- Workers AI의 일반 429, 용량 부족, 네트워크 오류와 의미 품질 실패에도 `GROQ_API_KEY`가 있으면 Groq로 전환한다.
- Groq 429·인증·잘못된 요청은 반복 재시도하지 않는다. 다만 앞선 시도에서 구조적으로 유효한 해석을 확보했다면 해당 응답을 반환한다.
- 두 공급자의 응답 모두 같은 Zod 스키마와 해석 품질 검사를 통과해야 한다.
- Groq 키는 `GROQ_API_KEY` Worker secret으로만 저장하며 브라우저, 문서와 Git에 포함하지 않는다.

## 6. 개인정보 및 안전 원칙

- 사용자의 질문, 선택 카드와 AI 응답을 서버에 저장하지 않는다.
- 완성된 결과는 사용자가 직접 저장을 선택한 경우에만 해당 브라우저의 IndexedDB에 저장한다.
- 선택형 이름은 브라우저 `localStorage`에만 저장하고 서버나 AI에 전송하지 않는다.
- D1에는 봇 방지와 호출 상한 확인에 필요한 익명 세션 해시, 만료 시각과 횟수만 저장하고 만료된 레코드는 삭제한다.
- AI 요청 원문, 사용자 질문, 생성 결과를 운영 로그에 남기지 않고, 검증 실패 시 시도 횟수와 고정된 오류 분류만 남긴다.
- 실명, 연락처, 계좌 정보 등 민감한 개인정보를 입력하지 않도록 화면에 안내한다.

### 6.1 오류 처리와 복구

오류가 발생해도 사용자가 입력한 질문, 선택 카드와 이미 공개한 상태를 가능한 한 유지한다. 오류 문구는 원인을 직접 설명하고 감성적이거나 타로식 표현을 사용하지 않는다.

| 상황 | 처리 방식 |
|---|---|
| 입력값 오류 | 잘못된 항목을 표시하고 입력 내용은 유지한다. |
| 오프라인 또는 네트워크 끊김 | 카드 선택 뒤라면 브라우저의 규칙 기반 해석으로 공개를 계속하고, 그 전 단계에서는 입력 상태를 유지한다. |
| Turnstile 검증 실패 | 입력 상태를 유지한 채 검증을 다시 실행한다. |
| 과다 요청 | HTTP 429와 남은 대기 시간을 표시하고 대기 후 재시도 버튼을 활성화한다. |
| 익명 세션 만료 | 질문과 카드 상태를 유지하고 Turnstile 재검증 후 새 세션으로 이어간다. |
| AI 응답 시간 초과 | 서버 또는 브라우저의 규칙 기반 해석으로 전환해 선택한 카드를 그대로 공개한다. |
| AI 구조화 응답 오류 | 짧은 보정 요청 뒤에도 기준을 통과하지 못하면 검증된 규칙 기반 해석으로 공개를 계속한다. |
| Workers AI 무료 한도 소진 | `GROQ_API_KEY`가 있으면 Groq로 한 번 전환하고 같은 스키마와 품질 검사를 적용한다. Groq도 사용할 수 없으면 가능한 계약만 규칙 기반으로 처리하고 열린 질문은 명확한 오류를 표시한다. |
| 의미 품질 검사 반복 실패 | 더 강한 Groq 120B로 보정하고, 구조가 정상인 마지막 응답이 있으면 문체·그래프 축 같은 부가 검사만으로 502를 반환하지 않는다. |
| 로컬 저장 공간 오류 | 화면의 결과는 유지하고 저장 실패와 텍스트 복사 대안을 안내한다. |

무료 사용량을 보호하기 위해 네트워크 오류나 시간 초과에 대해 AI 요청을 무제한 자동 재시도하지 않는다. 대체 해석도 동일한 질문, 카드, 자리와 정·역방향을 사용한다.

## 7. 1차 완성 목표와 제외 범위

기능을 최소 MVP로 축소하지 않고 아래 항목을 모두 갖춘 상태를 1차 완성 목표로 한다. 구현 순서는 나눌 수 있지만 완료 기준에서는 제외하지 않는다.

- 선택형 익명 이름과 이름 변경·삭제
- 자연어 질문 입력, 예시 질문과 입력 검증
- AI가 결정하는 최초 1~5장 리딩 구조
- 표준 78장 전체와 정·역방향 50% 배정
- 사용자의 직접 카드 선택, 순차 공개와 전체 공개
- 핵심 결론, 카드별 해석, 종합 해석과 질문별 확인 항목을 포함한 결과
- 동적 레이더 차트, 해석 신호 분포와 근거 카드 표시
- 선택형 후속 질문 최대 2회와 추가 카드 선택
- 후속 질문 전후 해석 지표 비교
- 진행 상태 복구와 선택형 로컬 결과 저장
- 로컬 기록 다시 보기, 개별 삭제와 전체 삭제
- 텍스트 복사, 사이트 주소 복사와 전체 결과 이미지 저장
- Turnstile, 서명 세션, 요청 제한과 입력 검증
- 모바일과 데스크톱 반응형 화면
- 키보드 조작, 명확한 포커스, 색상에 의존하지 않는 정보 표현과 모션 감소 설정
- 주요 오류 화면과 복구 동작
- 단위, 컴포넌트와 주요 사용자 흐름 자동 테스트

현재 범위에서 제외하는 항목은 다음과 같다.

- 로그인과 사용자 계정
- 서버에 저장되는 리딩 기록
- 공개 공유 URL과 사용자 간 공개 피드
- 유료 AI, 유료 데이터베이스와 유료 호스팅 기능
- 개인 도메인
- 후속 질문 2회를 넘는 장시간 대화

구현 중 확정할 세부 값은 정확한 흰색·검은색·회색 코드, 반응형 중단점과 최종 지원 브라우저 버전이다. 이 값들은 위 기능 범위를 변경하지 않는다.

## 8. 구현 상태와 다음 작업

2026-07-17 기준 운영 배포와 자동 검증 상태는 다음과 같다.

1. 완료: 78장 카드 의미 JSON과 79장 이미지 포함·무결성 확인
2. 완료: 리딩 설계, 최초·후속 해석과 그래프 응답 Zod 스키마
3. 완료: 질문, 구성 확인, 펼친 덱, 순차 공개, 결과, 후속 질문 화면
4. 완료: React·TypeScript·Vite/Vinext 프로젝트와 같은 출처 Worker API
5. 완료: Turnstile 검증 코드, HMAC 서명 세션, D1 원자적 호출 카운터와 Rate Limiting 바인딩 연결 코드
6. 완료: 선택형 로컬 기록, 새로고침 복구, 텍스트·주소 복사, 전체 결과 이미지 저장과 접근성 CSS
7. 완료: 단위·품질 테스트 95개, 빌드, 린트와 실제 브라우저 흐름 테스트
8. 완료: Cloudflare 계정 연결, D1·AI·Rate Limiting·Turnstile 바인딩과 비밀값 등록
9. 완료: 동일 입력으로 Workers AI 후보 모델을 비교하고 Gemma 4 26B-A4B를 기본 모델로 확정
10. 완료: 설명형 카드 결과를 포함한 320px·390px 모바일과 1280px 데스크톱 흐름 확인
11. 완료: `tarot-milktea` Worker 배포, 홈페이지·Turnstile 차단·실제 일정 및 식사 AI 해석 확인

## 9. 참고 자료

### 서비스 공식 문서

- [Cloudflare Workers AI 가격과 무료 할당량](https://developers.cloudflare.com/workers-ai/platform/pricing/)
- [Cloudflare Workers AI 모델 목록](https://developers.cloudflare.com/workers-ai/models/)
- [Cloudflare Workers AI Gemma 4 26B-A4B](https://developers.cloudflare.com/workers-ai/models/gemma-4-26b-a4b-it/)
- [Cloudflare Workers AI 호출 제한](https://developers.cloudflare.com/workers-ai/platform/limits/)
- [Cloudflare Workers 무료 한도](https://developers.cloudflare.com/workers/platform/limits/)
- [Cloudflare Workers AI 데이터 처리 정책](https://developers.cloudflare.com/workers-ai/platform/data-usage/)
- [Cloudflare Pages의 React 배포](https://developers.cloudflare.com/pages/framework-guides/deploy-a-react-site/)
- [Cloudflare Pages Function 바인딩](https://developers.cloudflare.com/pages/functions/bindings/)
- [Cloudflare Pages Turnstile 플러그인](https://developers.cloudflare.com/pages/functions/plugins/turnstile/)
- [Cloudflare Turnstile 요금제](https://developers.cloudflare.com/turnstile/plans/)
- [Cloudflare Worker Rate Limiting](https://developers.cloudflare.com/workers/runtime-apis/bindings/rate-limit/)
- [Cloudflare D1 요금과 무료 한도](https://developers.cloudflare.com/d1/platform/pricing/)
- [Groq 무료 API 호출 한도](https://console.groq.com/docs/rate-limits)
- [Groq GPT-OSS 120B 모델 정보](https://console.groq.com/docs/model/openai/gpt-oss-120b)
- [Groq 데이터 처리 정책](https://console.groq.com/docs/your-data)

### UI 및 시각화 참고 자료

- [AIsChoco](https://github.com/chococobyeol/AIsChoco) — 질문 유형에 따라 레이더 차트와 신호 표시 방식을 바꾸는 구성과 결과 화면 배치만 참고하며, 프롬프트와 해석 규칙은 참고하지 않는다.
- [타로시크릿 오늘의 운세](https://www.tarotcafe.net/tarot/today) — 전체 화면 장면 전환, 셔플, 선택지, 가로로 펼친 카드의 직접 선택 경험을 참고한다.
- [Destiny3D](https://destiny3d.com/) — 카드 뽑기를 읽는 페이지가 아닌 상호작용 무대로 제시하는 방향을 참고한다.
- [Virtual Tarot](https://www.virtualtarotapp.com/) — 화면 단위로 진행되는 앱형 리딩 흐름을 참고한다.
- [채아무 자료 아카이브](https://cha-amu.github.io/archive/) — 사용자 제작 카드 이미지 원본 출처로만 사용하며 블로그 레이아웃은 UI에 반영하지 않는다.
- [오뮤 다예쁨체](https://noonnu.cc/font_page/1136) — 전체 UI의 기본 글꼴과 라이선스 참고 자료.

### 카드 의미 데이터 참고 자료

- [Project Gutenberg: The Illustrated Key to the Tarot](https://www.gutenberg.org/ebooks/43548) — 공개 도메인 역사 자료로만 참고하고 원문을 그대로 복사하지 않는다.

## 10. 변경 기록

| 날짜 | 내용 |
|---|---|
| 2026-07-15 | Cloudflare Pages와 Workers AI를 사용하는 초기 기술 방향 기록 |
| 2026-07-16 | 카드 이미지 출처 확정 및 아카이브 이미지 79장 로컬 포함 |
| 2026-07-16 | 사용자 제작 이미지임을 확인하고 메이저·마이너 표준 78장 전체 사용 확정 |
| 2026-07-16 | 자연어 질문, AI 기반 가변 리딩 구조, 사용자의 직접 카드 선택 흐름 확정 |
| 2026-07-16 | 최초 리딩 카드 수를 1~5장으로 확정하고 후속 질문·추가 카드 흐름 추가 |
| 2026-07-16 | 질문 UI와 AI 해석에 감성적 수사를 배제하고 직접적·분석적 문체 사용 확정 |
| 2026-07-16 | 후속 질문을 선택 기능으로 두고 한 세션당 최대 2회로 제한 |
| 2026-07-16 | 후속 질문의 추가 카드 수를 고정하지 않고 질문에 따라 AI가 결정하도록 확정 |
| 2026-07-16 | 후속 질문당 추가 카드 수의 최대 상한을 5장으로 확정 |
| 2026-07-16 | 정방향과 역방향 카드를 모두 사용하기로 확정 |
| 2026-07-16 | 카드 방향을 정·역 50%로 배정하고 순차 공개와 전체 공개 기능을 함께 사용하기로 확정 |
| 2026-07-16 | AI 해석을 그래프와 수치 지표로 함께 보여주는 기능을 기획 범위에 추가 |
| 2026-07-16 | 3~5축 동적 레이더, 해석 신호 분포 막대, 후속 질문 전후 비교를 초기 시각화 방식으로 확정 |
| 2026-07-16 | 앱 이름을 `타로밀크티 웹`으로 확정하고 채아무 GitHub Pages의 분위기를 UI 참고 방향으로 지정 |
| 2026-07-16 | 전체 UI 기본 글꼴을 `오뮤 다예쁨체`로 확정하고 웹폰트 사용 및 라이선스 조건 기록 |
| 2026-07-16 | UI 색상을 흰색·검은색·회색 중심의 무채색 계열로 확정 |
| 2026-07-16 | Cloudflare 배포 이름을 `tarot-milktea`로 정하고 무료 주소는 실제 배포 후 확인하기로 정리 |
| 2026-07-16 | 카드 의미를 자체 작성한 78장 정·역방향 JSON 데이터로 관리하고 선택 카드 데이터만 AI에 제공하기로 확정 |
| 2026-07-16 | 구현 프레임워크를 React·TypeScript·Vite로 확정하고 Pages Function과 비공개 Worker API 구조 결정 |
| 2026-07-16 | 선택형 익명 이름, 로컬 기록, 결과 공유와 새로고침 복구 기능을 1차 완성 범위에 포함 |
| 2026-07-16 | Turnstile, 서명 익명 세션, D1 호출 카운터와 Worker Rate Limiting을 요청 보호 방식으로 확정 |
| 2026-07-16 | AI 해석 구성과 카드 수별 분량, 오류별 복구 방식을 확정하고 로그인은 현재 범위에서 제외 |
| 2026-07-16 | 전체 로컬 사이트, 78장 의미 데이터, Worker API, D1 세션 보호, 기록·공유·그래프와 자동 테스트 구현 완료 |
| 2026-07-16 | 블로그형 UI를 폐기하고 전체 화면 타로 게임 방식의 질문·셔플·직접 선택·순차 공개·탭형 결과 흐름으로 전면 재구성 |
| 2026-07-16 | 두들 타이틀 화면과 기본 이름 `ㅇㅁ` 추가, 밀크티 카드 뒷면 적용, 교차 셔플·78장 부채꼴 선택·카드 직접 공개·전체 폭 신호 분포로 개선 |
| 2026-07-16 | 홈에만 있던 장식선을 제거하고 전 장면을 미색 종이·검은 기능선·어긋난 단색 그림자의 두들 UI로 통일 |
| 2026-07-16 | 셔플을 좌우 덱 분리·교차 낙하·중앙 정렬의 3단계 동작으로 변경하고 건너뛰기 기능과 밝은 회색 카드 뒷면을 추가 |
| 2026-07-16 | 홈과 리딩 화면을 단일 Sand 중성색 스케일로 통일하고 `타로`·`밀크티` 제목 크기를 분리해 시각적 위계를 조정 |
| 2026-07-16 | 홈 제목을 `타로` 주 제목과 같은 줄의 작은 `밀크티` 보조명으로 정렬하고 카드 묶음 크기를 축소 |
| 2026-07-16 | 카드 선택 부채꼴을 완만한 원호로 조정하고 모바일에서 3겹 균등 배치·안전 여백을 적용해 양끝 잘림을 제거 |
| 2026-07-16 | 320px·390px 모바일과 1280px 데스크톱 전체 흐름을 확인하고 양끝 카드 선택 여백, 홈 세로 밀도, 소형 화면 결과 도구 표시를 보정 |
| 2026-07-16 | PC 카드 선택을 화면 중앙의 단일 깊은 부채꼴로 재구성하고 선택 카드가 원호에서 직접 부상하도록 조정 |
| 2026-07-16 | 홈·리딩 상단에 설정 버튼과 우측 설정 드로어를 추가하고 한국어·English 선택, 브라우저 저장, 새 AI 리딩 언어 반영을 구현 |
| 2026-07-16 | 카드 자리 트레이의 상·하 여백을 대칭으로 맞추고 슬롯 간격을 압축했으며, PC 덱을 카드 위치와 회전각이 일치하는 원호 곡선으로 보정 |
| 2026-07-16 | 셔플 종료 시 모든 카드 레이어를 완전히 정렬하고 마지막 카드가 아래로 튀며 윗면을 노출하던 동작을 제거했으며, 구성·해석 계산 장면에 최소 표시 시간을 적용해 화면 깜빡임 방지 |
| 2026-07-16 | 로컬·배포 Worker에 Workers AI `AI` 원격 바인딩을 연결하고 Qwen3 실제 질문 구성·카드 해석 응답의 스키마 검증 완료 |
| 2026-07-16 | 일상 질문에서 카드 원뜻을 단어 규칙으로 치환하던 방식을 폐기하고 원뜻→질문·자리 연결→판단 영향의 설명 구조와 근거 없는 인과 주장 거부 규칙을 추가 |
| 2026-07-16 | Workers AI 후보를 동일 조건으로 비교해 기본 해석 모델을 Gemma 4 26B-A4B로 교체 |
| 2026-07-16 | Cloudflare Worker `tarot-milktea`에 정적 화면·API를 배포하고 D1, Workers AI, Turnstile, 세션·네트워크 Rate Limiting을 운영 연결 |
| 2026-07-16 | 자연스러운 일정 표현과 그래프 축을 오거부하던 검증을 보정하고, 식후 만족·포만감의 근거 없는 인과는 차단한 채 실제 일정·식사 해석을 운영 주소에서 검증 |
| 2026-07-17 | `먹을지 … 먹을지` 형식의 두 선택지를 인식해 메뉴별 2장 구성, 첫 문장의 단일 결론, 해요체와 서버·브라우저 이중 대체 해석을 적용하고 선택→공개 흐름의 502 회귀를 수정 |
| 2026-07-17 | 질문별 고정 예외 처리를 일반 `answerContract`·`verdict` 구조로 교체하고, AI 의미 분류, 2~5개 명시 후보 보존, 비교·후속 문맥과 직접 결론 검증을 구현 |
| 2026-07-17 | 열린 추천의 사전 후보 생성을 폐지하고 `candidates=[]`인 역할 카드 구성 뒤 카드 공개 결과로 구체 답 하나를 생성하도록 변경 |
| 2026-07-17 | Workers AI의 정확한 일일 무료 한도 소진 시에만 Groq GPT-OSS로 전환하고, Strict JSON Schema·기존 품질 검사·출력 상한·비밀값 분리를 적용하는 대체 경로 추가 |
| 2026-07-17 | Groq 대체 경로의 구성 모델을 Qwen 3.6 27B로 확정하고 GPT-OSS 120B 해석·GPT-OSS 20B 1회 보정을 모델별 분당 한도로 분리했으며, 원인 질문의 구절을 선택 후보로 오인하던 공통 계약 판별을 수정 |
| 2026-07-17 | 열린 음식 추천의 반복·부자연스러운 문장과 간헐적 502를 함께 수정: 카드 의미를 식사 프로필로 번역하고 최근 추천 중복을 피하며, 품질 보정은 Groq 120B를 유지하고 구조적으로 유효한 마지막 응답을 보존 |
| 2026-07-17 | 성공·실패·합격·성사 여부를 후보 없는 `outcome` 계약으로 분리해 질문별 1~5장 역할을 유지하고 첫 문장에서 결과 한쪽을 직접 말하도록 변경 |
| 2026-07-17 | 맛·취향·친숙함·포만감·영양·몸 상태·날씨·상대 감정·미래 결과를 막던 도메인별 제약을 제거하고 카드 근거가 있는 타로 추론을 허용했으며 결과·복사·저장 이미지의 별도 고지 문구를 제거 |
